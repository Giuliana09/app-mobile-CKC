export interface IcorridaParametros {
    mes?: number;         
    kartodromo?: string;  
    pesquisa?: string;  // Nome da corrida ou dia (01 a 31)
    check?: boolean   // Listagem para mostrar corridas que tem Check-in feito
}